<aside class="bg-gray-100">
	<div class="py-24 container px-8 mx-auto">
		<div class="mb-16">
			<i class="inline-block font-mono text-blue-400 mb-4 lowercase">&#60; the_team &#47;&#62;</i>
			<h2 class="text-4xl capitalize">Conference organisers</h2>
			<p class="text-sm font-mono text-gray-700 max-w-3xl">With over 20 years combined event management experience, and over 30 years combined development experience, our organising team are excited to bring our 15th PHP conference to the UK. We organise several events in London, including both the <a href="http://gophercon.co.uk">Gophercon UK Conference</a> and the <a href="http://phplondon.org">PHP London</a> user group.</p>
		</div>
		{% for person in organisers.people %}
			<article class="p-4 mb-8 border">
				<header class="flex items-start md:items-center mb-4">
					<img class="rounded-full mr-4 w-16" src="{{person.avatar}}" alt="{{person.name}}"/>
					<div class="flex-grow">
						<h4 class="text-xl mb-0 capitalize">{{person.name}}</h4>
						<div class="text-gray-600 text-sm max-w-2xl">{{person.role}}</div>
					</div>
					<div class="text-right w-48">
						{% if person.links.twitter %}
							<a class="inline-block ml-4 w-8 text-blue-400 hover:text-blue-500 w-6" href="{{person.links.twitter}}" target="_blank">
								{% include 'components/icons/twitter.html' %}
							</a>
						{% endif %}
						{% if person.links.linkedin %}
							<a class="inline-block ml-4 w-8 text-blue-500 hover:text-blue-600 w-6" href="{{person.links.linkedin}}" target="_blank">
								{% include 'components/icons/linkedin.html' %}
							</a>
						{% endif %}
						{% if person.links.github %}
							<a class="inline-block ml-4 w-8 text-black hover:text-gray-700 w-6" href="{{person.links.github}}" target="_blank">
								{% include 'components/icons/github.html' %}
							</a>
						{% endif %}
					</div>
				</header>
				<p class="font-mono text-sm text-gray-800 max-w-4xl">{{person.bio}}</p>
				<div class="flex items-center flex-wrap">
					{% for link in person.links.websites %}
						<a class="font-mono text-sm mt-5 mr-5 text-blue-400 hover:text-blue-500" href="{{link.url}}">{{link.title}}</a>
					{% endfor %}
				</div>
			</article>
		{% endfor %}
	</div>
</aside>